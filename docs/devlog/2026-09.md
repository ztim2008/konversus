# Devlog — 2026-09

## 2026-09-10 — Этап 0: Harness · Loop · Graph

### Сделано

- `AGENTS.md`, `.cursor/rules/lead-radar-auto.mdc`
- `docs/guides/{HARNESS,LOOP,GRAPH}.md`
- `graph/system.graph.json` (taskPaths lead_radar_*)
- `scripts/harness-check.mjs`, `scripts/agent-loop.mjs`
- npm scripts `harness:check`, `agent:loop`
- `docs/CURRENT_CONTEXT.md`, этот devlog

### Как проверить

```bash
npm run harness:check
npm run agent:loop -- --task lead_radar_queue
```

## 2026-09-10 — Этап 1: модель данных и очередь

### Сделано

- `sql/005_lead_radar_auto.sql` накатана (platform, source, screenshot_*, kp_*, batches, новые статусы)
- Data-layer: `listQueuedSites`, `queue-site` helpers, `upsertBatchPlan`, KP/screenshot updaters
- API: `list-queue`, `queue-site`, `skip-site` + лимит 20
- Graph: узел `sql.lead_radar_auto`

### Как проверить

```bash
npm run harness:check
# MySQL: DESCRIBE lead_radar_sites; DESCRIBE lead_radar_batches;
```

### Дальше

Этап 2 — SERP + фильтр компаний.  
География v1 зафиксирована: СПб (bootstrap) → ротация Мск / Омск / В. Новгород.

## 2026-09-10 — Этап 2: Serper + фильтр

- `src/lib/serper-client.ts`, `company-site-filter.ts`, `lead-radar-geo.ts`
- `POST /api/secret-shopper/search-serp`
- Ключ только в `.env.local`
- Smoke: СПб «ремонт квартир» — статьи/profi отсекаются

### Дальше

Этап 3 — audit + deep email + screenshot.

## 2026-09-10 — Этап 3: audit + deep email + screenshot

- privacy/SEO эвристики в `website-checker`
- `deep-email.ts`, `site-screenshot.ts`
- `POST /api/secret-shopper/enrich-site`
- Smoke: prorabneva.ru — email + jpeg ~90KB

### Git

Push origin failed (repo access). Коммит локально.

## 2026-09-10 — Этап 4: HTML КП lead-web.pro

- `kp-html-template.ts` + AI промпт под lead-web.pro
- generate-kp возвращает html/subject/tokens
- send-email from lead-web.pro
- KpModal: HTML-превью

## 2026-09-10 — Этап 5: nightly + утренний дайджест

- `nightly-run.ts` + API + cron-скрипт
- Telegram текстовый дайджест
- Smoke: limit=1 → prorabneva.ru queued + screenshot + email
- Фильтр ложных email (@2x.webp)

## 2026-09-10 — Этап 6: админка «Сегодня»

- UI вкладка «Сегодня»: скрин, email, проблемы, превью КП, Отправить / Пропустить
- `send-queued.ts`: SMTP → contacted → batch.sent_count → TG «✅ Отправлено» + фото
- `skip-site` → skipped + batch.skipped_count, без SMTP
- Graph: `lib.send_queued`
- Smoke: list-queue=1, testMode send ok, skip без нового письма; лид возвращён в queued
- SMTP_* добавлены в `.env.local` (из окружения сервера)

### Дальше

Этап 7 — TG скрины утром + вечерний daily-report.

## 2026-09-10 — Этап 7: Telegram + daily-report

- Утренний дайджест: текст + до 5 sendPhoto по hotScore
- `daily-report.ts` + `POST /api/lead-radar/daily-report`
- Cron: `scripts/cron-lead-radar-daily-report.sh` (21:00 МСК)
- Правила: zero_sent → тихо; report_sent_at → идемпотентность
- Smoke: evening report ok; повтор already_sent; empty date zero_sent; фото скрина в TG ok
- Graph: `lib.daily_report`, `app.api_daily_report`, `ops.cron_daily_report`

### Дальше

Этап 8 — ответы → TG + админка.

## 2026-09-10 — Этап 8: ответы → TG + админка

- `mark-replied.ts` + TG «🔥 Ответ/заявка» (+ скрин)
- Авто-канал: CTA письма → `/api/lead-radar/interest` → replied → редирект lead-web.pro
- Webhook: `/api/lead-radar/reply-webhook` (form/n8n)
- Админка: фильтр «Ответили» + кнопка «🔥 Ответ»
- Smoke: mark-replied + TG ok; webhook idempotent; interest 302
- IMAP отложен
- Доп.: в TG уходит plain-текст КП (утро + при отправке); источники ответа на русском

### Дальше

Этап 9 — batches-дашборд / токены / полировка MVP.

## 2026-09-11 — Этап 9: статистика дней + MVP закрыт

- `config.ts`: DAILY_QUEUE_LIMIT=20 (nightly + API)
- API `list-batches` + live facts (= daily-report)
- UI вкладка «Статистика»: план/факт/токены/$/история
- Graph: `lib.lead_radar_config`
- DoD 🟩 → MVP Auto закрыт

## 2026-09-11 — Закрытие дня (после этапов 6–9 + TG polish)

### Сделано сегодня (сводка)

- Этап 6: админка «Сегодня» — Отправить / Пропустить  
- Этап 7: TG скрины + daily-report 21:00  
- Этап 8: ответы (CTA interest + webhook + кнопка)  
- Этап 9: статистика дней / токены / лимит 20  
- TG группа «Лиды КОНВЕРСУС» подключена  
- Plain-текст КП в дайджесте/отправке (без токенов AI); источники ответа по-русски  
- DeepSeek через OpenRouter; SMTP в `.env.local`

### Завтра

Прогон системы + анализ узких ниш / рулетка жирных клиентов (стройка·ремонт·услуги·производство).  
Промпт сессии: `docs/prompts/2026-09-11-lead-radar-polish.md`

### Не сделано / бэклог

- Кнопки Отправить/Пропуск в Telegram-боте  
- IMAP ответов на email  
- Расширение словаря ниш / вертикалей (план завтра)
